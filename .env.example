# OpenAI Director/transcription (billed). Copy to .env — gitignored.
# OPENAI_API_KEY=sk-...

# Milestone 3 image generation (billed per image; shares OPENAI_API_KEY).
# WTS_IMAGE_MODEL=gpt-image-1
# Max generated stills per visual-direction pass.
# WTS_IMAGE_BUDGET=8
# Set to "off" to skip vision review and frame sampling during builds.
# WTS_VISUAL_QA=on
