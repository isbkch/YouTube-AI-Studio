# Analytics review resolution

`ocr review` completed successfully after implementation and local acceptance. Session: `d52327c4-4a15-4f60-bfbb-0286d4a54065`. It reviewed 22 source files and returned 18 findings. The captured command output is in `.cache/analytics-validation/ocr-review.txt` in the implementation worktree.

Findings were handled from high severity downward. Two reported Swift compile failures were not reproducible: the release executable had already compiled. Their suggested explicit conversions were nevertheless applied for clarity and compiled again. The other 16 findings were corrected.

| Priority | Finding                                                    | Resolution and evidence                                                                                                                                                                                                                                       |
| -------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| High     | Transient OAuth failures could erase cached evidence       | 429/5xx token responses are retryable external errors; credentials and reports survive. Regression covers 429, 500 and 503 alongside genuine authorization failure.                                                                                           |
| High     | Reversed collection allegedly failed to compile            | Prior build disproved the failure; return now explicitly constructs `Array`. Subsequent Swift release tests compile the views.                                                                                                                                |
| High     | String/Substring concatenation allegedly failed to compile | Prior build disproved the failure; opportunity IDs now explicitly construct `String`. Subsequent Swift release tests compile the views.                                                                                                                       |
| Medium   | Swift optional fields omitted required JSON nulls          | Payload encoding inserts explicit nulls for nullable video, outcome, review and topic fields. Three native Swift tests exercise this IPC boundary.                                                                                                            |
| Medium   | Project transaction could leave orphan files               | Prepare a pure project document, commit project/event/topic rows together, then materialize directories and snapshots. Injected DB failure leaves no project row or directory; file failure repairs on idempotent retry.                                      |
| Medium   | CSV artifact preceded import transaction                   | Write the raw artifact after the channel/report/import transaction commits. Injected DB failure leaves no raw import directory; retry succeeds.                                                                                                               |
| Medium   | Analytics owned global schema version                      | The top-level `migrations.ts` owns ordered library migrations and the global version. Analytics supplies table DDL only. Existing migration/compatibility tests pass.                                                                                         |
| Medium   | Expiry stranded independent creator records                | Retained reviews remain visible without source metadata and can generate a follow-up from the creator's lesson. Existing outcomes can be edited while retaining an unavailable-source association. Regression covers expiry, follow-up and stage progression. |
| Medium   | Download bound applied after full allocation               | Reach downloads enforce Content-Length and a streaming byte limit before constructing CSV text. Tests cover declared and chunked oversized bodies and cancellation.                                                                                           |
| Medium   | Generated claims checked only in some fields               | Every generated prose field is checked for unsupported measured/causal claims. Parameterized coverage exercises all eight fields.                                                                                                                             |
| Medium   | Duplicate evidence overstated support                      | Deduplicate evidence IDs before assigning support strength. Regression prevents a repeated ID from remaining Supported.                                                                                                                                       |
| Medium   | Missing OAuth client produced a raw schema error           | Return a configuration error directing the creator to Desktop OAuth JSON. Regression covers a fresh vault.                                                                                                                                                    |
| Medium   | Wrong OAuth JSON shape produced a raw schema error         | Safe-parse and provide the same actionable Desktop-client guidance. Regression covers a web-client file.                                                                                                                                                      |
| Medium   | Imported title bypassed model bounds                       | Validate videos and reports against their persisted schemas during preview. Oversized titles fail before any commit.                                                                                                                                          |
| Medium   | Channel hid active production progress/cancel              | Keep the existing project status banners above the Channel workspace. Native compilation verifies integration.                                                                                                                                                |
| Medium   | Review evidence matched only the end date                  | Require both the public start date and the review end date, matching the domain queue's exact-window rule.                                                                                                                                                    |
| Low      | Missing positional CSV path produced a Node error          | Validate the argument and show the full import command syntax. Regression covers missing path.                                                                                                                                                                |
| Low      | Sample retained stale error/notice                         | Clear prior feedback before loading the sample channel.                                                                                                                                                                                                       |

Final verification commands: `bun run check`, changed-file Prettier checks, `git diff --check`, `swift test --package-path apps/macos -c release`, and `swiftc -typecheck scripts/analytics-keychain.swift`. Live OAuth/provider acceptance remains a separate pilot gate as described in [analytics.md](analytics.md).

Post-review native checks also passed: a channel-level outcome saved with no video/topic association, and an imported video saved updated tags while its public date and duration remained unverified. Final totals: 164 TypeScript tests and 3 Swift tests passed.
