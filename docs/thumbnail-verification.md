# Thumbnail variants verification

September 17, 2026. Implementation branch: `codex/thumbnail-variants`, separate checkout: `../yt-studio-thumbnails`.

## Scope delivered

Two persisted thumbnail variants in Review → Packaging, rendered through the existing image provider with local exact-headline composition. Native large/feed comparison, full-size preview, headline/direction editing, revision history, explicit background regeneration, pinned upload selection, and exact A/B export. Publication approval includes the selected image descriptor and hash. Live A/B experiments remain in YouTube Studio.

## Verification

- `bun run check`: typecheck, lint, and all 150 tests passed after the review corrections (147 before review). The worktree needed the existing ignored `docs/video-script-example.md` fixture copied from the original checkout for the pre-existing pre-production test.
- `node --import tsx --test tests/thumbnails.integration.ts`: passed with the real Remotion still composition and FFmpeg decode. Covered two finished 1280×720 JPEGs, overflow rejection, retained background on failure, offline reuse and exact export hashes. No paid image provider.
- `swift build --package-path apps/macos -c release`: passed. Existing warnings remain in unrelated native views.
- `bun run format:check` and `git diff --check`: passed.
- Native UI checked using a separately signed `Thumbnail Verification.app` with a unique bundle ID, runtime rooted in this worktree and a disposable library under `.demo/thumbnail-verification`. The installed app and normal library were not replaced.
- Native checks: both images visible in comparison, large/feed modes, selection changes, selection surviving restart, full-image preview, headline edit producing revision 2, previous-revision preview, native export picker and successful export. The editor was corrected after native verification showed multiline text clipping; the corrected layout was verified visually.
- After review corrections, the rebuilt native app displayed both images with the asynchronous image loader. A deliberately interrupted fixture was recovered through **Recover interrupted render**, retried successfully to A revision 3, and kept B revision 1 pinned for upload. Read-only state matches SQLite; recovery persists under the existing project lock.
- Export from the native UI contained A revision 2 and B revision 1. Both JPEG hashes matched their manifest. The integration export also retained its original A/B revision 1 bytes.
- Domain/IPC/CLI tests cover partial failure and retry, cancellation, background retention and usage, stale requests, persisted recovery, packaging history, optional thumbnails, exact approved upload argument, changed/missing/symlinked files, frozen published choices, conflicting thumbnail arguments and video-created/thumbnail-failed recovery without duplicate upload.

Retained local evidence is in `.demo/thumbnail-check.log`, `.demo/thumbnail-integration.log`, `.demo/thumbnail-swift-build.log`, `.demo/thumbnail-format.log` and `.demo/thumbnail-verification/`. These are disposable, ignored artifacts.

## Requested code review

Ran `ocr review` after implementation, tests and native verification were complete. It exited successfully after reviewing 19 included files and reported 20 findings, all medium or low severity. Session: `38693555-46b9-43e1-adf8-5bd5b532e99d`. The full captured report is `.demo/ocr-review.log`. Corrections were handled in severity order, beginning with the stream callback that could crash the runtime.

| Severity | Finding                                                | Resolution                                                                                                                                                                                                                                       |
| -------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Medium   | Upload callback exception escapes stream handler       | Catch inside the handler, retain the created video ID and report the persistence warning; the caller can persist the final result. Added regression test.                                                                                        |
| Medium   | Injected renderer reuses default renderer's cache      | Cache identity comes from the actual renderer; unidentified renderers bypass reuse. Added renderer-switch regression test.                                                                                                                       |
| Medium   | No-op edits invalidate ready images                    | Unchanged inputs preserve status, revision token and export eligibility. Tested.                                                                                                                                                                 |
| Medium   | Interrupted state exists only in returned clone        | Reads now return persisted state; explicit native recovery uses the existing locked domain operation. Tested in domain and native app.                                                                                                           |
| Medium   | Renderer accepts unsupported background magic          | Validate complete PNG/JPEG signatures before browser work. Added direct-renderer invalid-input tests.                                                                                                                                            |
| Medium   | Hard-coded Remotion version in cache key               | Derive version from the installed Remotion export. Real integration render passed.                                                                                                                                                               |
| Medium   | Native image reads and decoding during body evaluation | Load/decode asynchronously into state keyed by project/path/hash. Rechecked native comparison visually.                                                                                                                                          |
| Medium   | Comparison sheet accepts an older packaging document   | Match the current packaging version before exposing images/actions. Swift build passed.                                                                                                                                                          |
| Medium   | CLI suppresses image credential errors                 | Use strict image-only setup when generation is necessary; saved backgrounds still work offline. Provider tests passed. Also passed cancellation through CLI render/regenerate.                                                                   |
| Medium   | Render A/B concurrently                                | Intentionally retained sequential rendering from the approved plan: it limits simultaneous paid requests, permits cancellation before B starts, and preserves independent partial success. Existing cancellation and partial-failure tests pass. |
| Low      | Background read ignores abort signal                   | Pass the signal to `readFile`.                                                                                                                                                                                                                   |
| Low      | Missing image throws raw filesystem error              | Convert missing/unreadable bytes to the same actionable conflict as changed bytes; missing-file regression assertion passed.                                                                                                                     |
| Low      | CLI export without destination throws TypeError        | Validate destination type and absolute path in the domain; CLI regression passed.                                                                                                                                                                |
| Low      | Implicit upload result type                            | Use `Awaited<ReturnType<typeof publishToYouTube>>`. Typecheck passed.                                                                                                                                                                            |
| Low      | Nested provider ternary                                | Replace with explicit branches.                                                                                                                                                                                                                  |
| Low      | Nested publication-status ternary                      | Compute the typed status explicitly.                                                                                                                                                                                                             |
| Low      | Two-word mock headline                                 | Use “IT WORKS UNTIL PROD.” to match the prompt's three-to-six-word guidance.                                                                                                                                                                     |
| Low      | Hard-coded integration edit version                    | Read the fixture's current packaging version.                                                                                                                                                                                                    |
| Low      | Hard-coded integration export version                  | Use the same current packaging version.                                                                                                                                                                                                          |
| Low      | Prototype headline lacks conventional CSS fallback     | Add a `clamp()` fallback before `cqw`. Formatting passed.                                                                                                                                                                                        |

Post-review validation: all 150 standard tests, the real thumbnail integration test, Swift release compilation, formatting and native recovery/comparison checks passed. No unresolved correctness finding remains; the one performance suggestion above was declined to preserve the approved interaction and cost scope.

## Limits

Pull request preparation, September 18, 2026: rebased onto `main` at `19407d0` (Producer autonomy), retaining both sets of native models, project normalization and creator publication gates. Fresh validation passed: `bun run check` (160 tests), the real thumbnail integration render, Swift release compilation, formatting and diff checks. Native interaction checks above were performed before this rebase; the combined native sources were compiled afterward.

Image generation was tested with the deterministic mock; no live OpenAI/Gemini call was made. Upload behavior was tested with a fake local CLI; no video was uploaded and no live YouTube experiment was created. Creator review remains necessary for the generated subject, exact text and small-size readability.
